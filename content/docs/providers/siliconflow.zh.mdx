---
title: SiliconFlow
description: Agent 怎样通过 KeyKeeper 拿到 SiliconFlow 的 key，在 cloud.siliconflow.cn 该选什么，KeyKeeper 怎么验证。
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

| 模板 id | `siliconflow`（别名 `硅基流动`、`siliconflow-cn`） |
|---|---|
| 字段 | `siliconflow-api-key` → `keykeeper run` 里的 `SILICONFLOW_API_KEY` |
| key 长什么样 | 以 `sk-` 开头，至少 40 个字符，可在控制台再次查看 |
| 创建页面 | [cloud.siliconflow.cn](https://cloud.siliconflow.cn/account/ak) |
| KeyKeeper 的验证 | `GET https://api.siliconflow.cn/v1/models`——列出这把 key 能用的模型 |
| 模板核实日期 | 2026-09-15 |

## Agent 做什么

```bash
keykeeper providers show siliconflow
keykeeper save --provider siliconflow --from-clipboard --create --purpose "这个任务是做什么的"
keykeeper run -c siliconflow -- <你的命令>
```

`--provider siliconflow` 会补上凭据 ID 和字段名。这个字段的环境变量就是 SiliconFlow 自家工具读的那个，不需要任何映射。

## 只有你能做的

<Steps>
<Step>手机号或邮箱登录</Step>
<Step>部分模型需要先实名认证</Step>
<Step>复制 key，然后在 KeyKeeper 的窗口里确认保存。</Step>
</Steps>

## 该选什么

key 没有权限范围：一把 key 就是整个账号。每个项目建一把，方便单独删除。

<Callout type="info">
Agent 被告知的是同一件事。它替你打开页面、说明该选什么；它从不替你登录、过 2FA 或付费。
</Callout>

## KeyKeeper 检查什么

- **写入前：** 值必须像一把 SiliconFlow 的 key（以 `sk-` 开头，至少 40 个字符）。贴错了会在存入之前被拒绝，并告诉 Agent 原因——不含值。
- **保存后：** KeyKeeper 自己发送 `GET https://api.siliconflow.cn/v1/models`，key 放在 `Authorization` 请求头里（列出这把 key 能用的模型）。Agent 收到的是「接受」「拒绝」或「联系不上」——从不是值。

## 轮换与过期

没有过期设置；轮换就是删掉重建。 在 [cloud.siliconflow.cn](https://cloud.siliconflow.cn/account/ak) 轮换或撤销。
