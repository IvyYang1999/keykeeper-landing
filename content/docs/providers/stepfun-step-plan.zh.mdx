---
title: StepFun · Step Plan
description: Agent 怎样通过 KeyKeeper 拿到 StepFun · Step Plan 的 key，在 platform.stepfun.ai 该选什么，KeyKeeper 怎么验证。
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

| 模板 id | `stepfun-step-plan`（别名 `Step Plan`、`StepFun coding plan`） |
|---|---|
| 字段 | `step-api-key` → `STEP_API_KEY` · 密钥 |
| key 长什么样 | 可在控制台再次查看 |
| 创建页面 | [platform.stepfun.ai](https://platform.stepfun.ai/interface-key) |
| KeyKeeper 的验证 | 不验证 |
| 模板核实日期 | 2026-09-15 |

## Agent 做什么

```bash
keykeeper providers show stepfun-step-plan
keykeeper save --provider stepfun-step-plan --from-clipboard --create --purpose "这个任务是做什么的"
keykeeper run -c stepfun-step-plan -- <你的命令>
```

`--provider stepfun-step-plan` 会补上凭据 ID 和字段名。这些环境变量就是 StepFun 阶跃 自家工具读的那个，不需要任何映射。

## 只有你能做的

<Steps>
<Step>登录 StepFun Open Platform</Step>
<Step>先订阅或激活 Step Plan</Step>
<Step>再创建用于 Step Plan 的 API Key</Step>
<Step>复制 key，然后在 KeyKeeper 的窗口里确认保存。</Step>
</Steps>

## 该选什么

Step Plan 使用专属套餐/API Key，但与标准 API 共用兼容协议路径且无需额外前缀；调用前确认 key 所属套餐有效。

<Callout type="info">
Agent 被告知的是同一件事。它替你打开页面、说明该选什么；它从不替你登录、过 2FA 或付费。
</Callout>

## KeyKeeper 检查什么

- **写入前：** 值必须像一把 StepFun · Step Plan 的 key（没有形状规则）。贴错了会在存入之前被拒绝，并告诉 Agent 原因——不含值。
- **保存后：** StepFun · Step Plan 没有 KeyKeeper 可用的只读接口，第一次真实调用就是测试。


## 接入地址

这把 key 是下面这些地址的 bearer token。`keykeeper run` 只设置上面的环境变量，base URL 要你自己在客户端里指。

- **OpenAI compatible**——`https://api.stepfun.ai/v1`
- **Anthropic compatible**——`https://api.stepfun.ai`

## 轮换与过期

未确认 API Key 自身的统一到期时间；套餐额度按月清零，套餐取消后服务持续至当前计费周期结束。 在 [platform.stepfun.ai](https://platform.stepfun.ai/interface-key) 轮换或撤销。
