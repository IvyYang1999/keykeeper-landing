---
title: 阿里云百炼 · 按量 (新加坡)
description: How an agent gets a 阿里云百炼 · 按量 (新加坡) key through KeyKeeper, what to choose on bailian.console.aliyun.com, and how KeyKeeper verifies it.
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

| Template id | `alibaba-bailian-sg` |
|---|---|
| Field | `dashscope-api-key` → `DASHSCOPE_API_KEY` · secret |
| Key looks like | whether it can be viewed again is unconfirmed |
| Created at | [bailian.console.aliyun.com](https://bailian.console.aliyun.com/) |
| Verified by KeyKeeper | not verified |
| Template checked | 2026-09-15 |

## What the agent does

```bash
keykeeper providers show alibaba-bailian-sg
keykeeper save --provider alibaba-bailian-sg --from-clipboard --create --purpose "what this task does"
keykeeper run -c alibaba-bailian-sg -- <your command>
```

`--provider alibaba-bailian-sg` fills in the credential id and the field. The environment
variable is the one 阿里云百炼's own tools read, so nothing has to be mapped.

## What only you can do

<Steps>
<Step>登录阿里云，选择 新加坡（ap-southeast-1）与对应业务空间</Step>
<Step>在 API Key 管理中创建该地区的按量 key；不要复制 Coding / Token Plan key</Step>
<Step>配置同地区 endpoint；业务空间专属域名需从控制台确认 WorkspaceId</Step>
<Step>Copy the key, then approve the save in KeyKeeper's window.</Step>
</Steps>

## What to choose

不同地区的 key 不通用。优先独立业务空间、限制模型与 IP 的自定义权限。

<Callout type="info">
The agent is told the same thing. It opens the page for you and says what to pick; it never
logs in, passes 2FA or pays on your behalf.
</Callout>

## What KeyKeeper checks

- **Before writing:** the value must look like a 阿里云百炼 · 按量 (新加坡) key (whether it can be viewed again is unconfirmed). A wrong paste is refused before anything is stored, and the agent is told why — without the value.
- **After saving:** 阿里云百炼 · 按量 (新加坡) offers no read-only endpoint KeyKeeper can use, so the first real call is the test.


## Endpoints

The key is a bearer token for these endpoints. `keykeeper run` sets only the variables above;
point your client at the base URL yourself.

- **OpenAI** — `https://dashscope-intl.aliyuncs.com/compatible-mode/v1` (ap-southeast-1)
- **Anthropic** — `https://dashscope-intl.aliyuncs.com/apps/anthropic` (ap-southeast-1)
- **DashScope** — `https://dashscope-intl.aliyuncs.com/api/v1` (ap-southeast-1)
- **OpenAI · workspace (replace WorkspaceId)** — `https://{WorkspaceId}.ap-southeast-1.maas.aliyuncs.com/compatible-mode/v1` (ap-southeast-1)

## Rotation and expiry

本模板管理持久 API Key，无固定有效期；不是最多 1800 秒的临时 key。仍可能被撤销。 Rotate or revoke at [bailian.console.aliyun.com](https://bailian.console.aliyun.com/).
