---
title: Compshare · ModelVerse (Global)
description: How an agent gets a Compshare · ModelVerse (Global) key through KeyKeeper, what to choose on console.compshare.cn, and how KeyKeeper verifies it.
---

import { Step, Steps } from "fumadocs-ui/components/steps";
import { Callout } from "fumadocs-ui/components/callout";

| Template id | `compshare-modelverse-global` (also `umodelverse`, `compshare-global`) |
|---|---|
| Field | `compshare-api-key` → `COMPSHARE_API_KEY` · secret |
| Key looks like | can be viewed again in the console |
| Created at | [console.compshare.cn](https://console.compshare.cn/) |
| Verified by KeyKeeper | not verified |
| Template checked | 2026-09-15 |

## What the agent does

```bash
keykeeper providers show compshare-modelverse-global
keykeeper save --provider compshare-modelverse-global --from-clipboard --create --purpose "what this task does"
keykeeper run -c compshare-modelverse-global -- <your command>
```

`--provider compshare-modelverse-global` fills in the credential id and the field. The environment
variable is the one Compshare's own tools read, so nothing has to be mapped.

## What only you can do

<Steps>
<Step>登录 Compshare 控制台</Step>
<Step>进入 ModelVerse API Key 管理</Step>
<Step>创建项目专用密钥</Step>
<Step>仅在 .cn 入口不可达时选择官方海外入口</Step>
<Step>Copy the key, then approve the save in KeyKeeper's window.</Step>
</Steps>

## What to choose

普通 ModelVerse 按量密钥；海外域名是官方备用访问地址，不是 Agent Plan。按项目拆分密钥。

<Callout type="info">
The agent is told the same thing. It opens the page for you and says what to pick; it never
logs in, passes 2FA or pays on your behalf.
</Callout>

## What KeyKeeper checks

- **Before writing:** the value must look like a Compshare · ModelVerse (Global) key (can be viewed again in the console). A wrong paste is refused before anything is stored, and the agent is told why — without the value.
- **After saving:** Compshare · ModelVerse (Global) offers no read-only endpoint KeyKeeper can use, so the first real call is the test.


## Endpoints

The key is a bearer token for these endpoints. `keykeeper run` sets only the variables above;
point your client at the base URL yourself.

- **OpenAI compatible** — `https://api.umodelverse.ai/v1` (Global fallback)
- **Anthropic Messages** — `https://api.umodelverse.ai` (Global fallback)
- **Gemini compatible** — `https://api.umodelverse.ai` (Global fallback)

## Rotation and expiry

未确认统一有效期；以创建页面显示的实际日期为准，未知时不要猜测。 Rotate or revoke at [console.compshare.cn](https://console.compshare.cn/).
